#ifndef _SIGN_KEYPAIRHFE_HPP
#define _SIGN_KEYPAIRHFE_HPP

#include "config_HFE.h"

/* Generates a keypair - pk is the public key and sk is the secret key. */
int sign_keypairHFE(UINT* pk,UINT* sk);


#endif
