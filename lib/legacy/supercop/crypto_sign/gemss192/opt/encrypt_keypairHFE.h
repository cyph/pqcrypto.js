#ifndef _ENCRYPT_KEYPAIRHFE_H
#define _ENCRYPT_KEYPAIRHFE_H

#include "config_HFE.h"

/* Generates a keypair - pk is the public key and sk is the secret key. */
int encrypt_keypairHFE(UINT* pk,UINT* sk);


#endif
